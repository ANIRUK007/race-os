#!/usr/bin/env bash
# Build step. Runs after every pull of main, before the service restarts.
# Nothing to build yet -- put dependency installs / compilation here.
set -euo pipefail
echo "deploy.sh: nothing to build"
